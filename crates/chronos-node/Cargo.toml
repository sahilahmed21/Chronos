[package]
name = "chronos-node"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Production interpreter: real TCP, files, SystemTime."

[dependencies]
chronos-protocol = { path = "../chronos-protocol" }

[lints]
workspace = true
