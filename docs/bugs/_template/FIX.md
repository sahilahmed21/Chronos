If PROTOCOL: patch summary and `run_seed(<SEED>)` now returns `check: None`.

If LIMITATION: cite D10 / PAR / sofa-jraft. Do not patch Raft to survive lying fsync in v1.

If PLANTED: not found in random swarm of N seeds; planted `skip_vote_persist` to prove the harness. Swarm does not enable the hook.
