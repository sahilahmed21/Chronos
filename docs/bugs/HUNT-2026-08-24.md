# Hunt log — 2026-08-24

**Status:** CLOSED 2026-08-25. CLASS=**CHECKER** (not PROTOCOL). PLANTED pack remains the G9 harness proof.

Local Docker on `cd35c46` / tree matching green CI (Windows host, `rust:1-bookworm`).

## Commands (hunt)

```text
docker run --rm -v c:/projects/Chronos:/work -w /work -e RUST_TEST_THREADS=1 \
  rust:1-bookworm cargo test --workspace --release
# → green (same bar as CI verify Test step)

docker run --rm -v c:/projects/Chronos:/work -w /work rust:1-bookworm \
  cargo run -p chronos-sim --release -- --seeds 1000 --start 1 --out traces --coverage
# → 1/1000 seeds failed (exit 1)  [pre-fix]
```

## Hunt result (pre-fix)

| Item | Value |
|------|--------|
| Failed seed | `281` |
| Check | `Linearizability` |
| Detail | `no linearization of the client history` |
| Profile | brutal, n=3 |
| Digest | `cca2e7dcd3a609f5891e38bd9b0908c065d848cd6b70517c3d660acb14fae0e7` |
| `--seed 281` | FAIL (same digest) |
| `--replay traces/fail-281.trace` | `REPRODUCED` |
| `--minify --seed 281` | `MINIFY MISMATCH` at hunt time (full recorded schedule did not reproduce) |

## Close (2026-08-25)

Triage: HARNESS (heartbeat delay not in `ReplayBook`) then CHECKER (`Io` treated as a definite miss).

After fixes: `--seed 281` **ok** (same digest); `--minify --seed 281` **MINIFY CLEAN**; `--seeds 1000 --start 1 --out traces --coverage` **exit 0**, 0 CheckFails (re-run 2026-08-25, 1000 `ok` lines, coverage table printed).

No PROTOCOL pack. `skip_vote_persist` never enabled in swarm/CLI minify.

Full playbook: [CLOSE-281.md](CLOSE-281.md).
